## Hibernate

# Dialect
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Show SQL queries in the log
spring.jpa.show-sql = true
spring.jpa.generate-ddl=true

# Automatically update the database schema based on Java entities
spring.jpa.hibernate.ddl-auto = update


## MySQL

# Connection URL
#spring.datasource.url=jdbc:mysql://test:3306/testdb

# Credentials
spring.datasource.username=root
spring.datasource.password=root

## External Hospital service
hospital.client.url=http://localhost:8089/cities/%s/hospitals